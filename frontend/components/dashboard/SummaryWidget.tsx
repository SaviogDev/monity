'use client'; import { motion } from 'framer-motion';
import { ArrowUpRight, ArrowDownRight, Wallet, TrendingUp, TrendingDown, CreditCard as CardIcon } from 'lucide-react';
import { ReactNode } from 'react'; const itemVariants = { hidden: { opacity: 0, y: 15 }, show: { opacity: 1, y: 0 }
}; interface MetricCardProps { title: string; value: string | number; subtitle: string; trend: 'up' | 'down' | 'neutral'; tone: 'blue' | 'green' | 'red' | 'purple'; icon: ReactNode;
} function MetricCard({ title, value, subtitle, trend, tone, icon }: MetricCardProps) { const styles = { blue: 'bg-[var(--bg-card)] dark:bg-[#101216] border border-[var(--border)]/60 dark:border-white/5 shadow-sm', green: 'bg-[var(--bg-card)] dark:bg-[#101216] border border-[var(--border)]/60 dark:border-white/5 shadow-sm', red: 'bg-[var(--bg-card)] dark:bg-[#101216] border border-[var(--border)]/60 dark:border-white/5 shadow-sm', purple: 'bg-[var(--bg-card)] dark:bg-[#101216] border border-[var(--border)]/60 dark:border-white/5 shadow-sm', }[tone]; const iconStyles = { blue: 'bg-blue-50 dark:bg-blue-500/10 text-blue-600 dark:text-blue-400', green: 'bg-emerald-50 dark:bg-emerald-500/10 text-emerald-600 dark:text-emerald-400', red: 'bg-rose-50 dark:bg-rose-500/10 text-rose-600 dark:text-rose-400', purple: 'bg-purple-50 dark:bg-purple-500/10 text-purple-600 dark:text-purple-400', }[tone]; const TrendIcon = trend === 'up' ? ArrowUpRight : ArrowDownRight; const trendColor = trend === 'up' ? 'text-emerald-500' : trend === 'down' ? 'text-rose-500' : 'text-slate-400'; return ( <motion.div variants={itemVariants} className={`flex flex-col justify-between rounded-[2rem] p-7 transition-all hover:scale-[1.02] ${styles}`}> <div className="flex items-center justify-between"> <div className={`flex h-12 w-12 items-center justify-center rounded-2xl shadow-inner ${iconStyles}`}> {icon} </div> <div className={`flex items-center gap-1 rounded-full bg-[var(--bg-card)] dark:bg-black/20 px-2 py-1 text-[10px] font-black ${trendColor}`}> <TrendIcon size={12} /> {subtitle} </div> </div> <div className="mt-6"> <p className="text-[10px] font-black uppercase tracking-[0.24em] text-slate-400 dark:text-slate-400"> {title} </p> <p className="mt-1 truncate text-3xl font-black tracking-tighter text-slate-900 dark:text-white sm:text-4xl"> {value} </p> </div> </motion.div> );
} interface SummaryWidgetProps { metrics: { income: { value: string; trend: 'up' | 'down'; percent: string }; expense: { value: string; trend: 'up' | 'down'; percent: string }; balance: { value: string; trend: 'up' | 'down'; percent: string }; projection: { value: string; trend: 'up' | 'down'; percent: string }; };
} export function SummaryWidget({ metrics }: SummaryWidgetProps) { return ( <div className="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-4"> <MetricCard title="Entradas (Mês)" value={metrics.income.value} subtitle={metrics.income.percent} trend={metrics.income.trend} tone="green" icon={<TrendingUp size={24} />} /> <MetricCard title="Saídas (Mês)" value={metrics.expense.value} subtitle={metrics.expense.percent} trend={metrics.expense.trend === 'up' ? 'down' : 'up'} tone="red" icon={<TrendingDown size={24} />} /> <MetricCard title="Saldo Líquido" value={metrics.balance.value} subtitle={metrics.balance.percent} trend={metrics.balance.trend} tone="blue" icon={<Wallet size={24} />} /> <MetricCard title="Projeção Final" value={metrics.projection.value} subtitle={metrics.projection.percent} trend={metrics.projection.trend} tone="purple" icon={<CardIcon size={24} />} /> </div> );
}
